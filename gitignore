# Environment & credentials — JANGAN pernah di-commit
.env
*.env

# Data raw — file besar, simpan di Google Drive saja
RAW_SYNOP_REPORT.csv
data/raw/

# Output CSV — sudah ada di database, tidak perlu di repo
data/output/
*.csv

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Jupyter checkpoints
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db
